<h1><%= @task.title %></h1>
<p><strong>Task:</strong> <%= @task.details %></p>
<p>This task is <u><%= @task.completed ? "completed" : "not completed" %></u>.</p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<br>
<%= link_to "Back to List", "/tasks" %>
<br>
<%= link_to "Edit this task", "./#{@task.id}/edit" %>
